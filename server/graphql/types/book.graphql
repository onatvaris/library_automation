type Query {
  book(id: ID!): Book!
  books: [Book]
}

type Book {
  id: ID!
  title: String!
  description: String
  year: Int
}

type Mutation {
  addBook(title: String!, description: String, year: Int): Book!
  deleteBook(id: ID!, title: String): Book
}
